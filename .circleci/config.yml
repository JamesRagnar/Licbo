version: 2

jobs:
  test:

    macos:
      xcode: "9.0"

    steps:
      - checkout

      - restore_cache:
          keys:
            - v3-{{ .Branch }}

      - run:
          name: Ruby
          command: chruby 2.3.1

      - run: 
          name: Bundler
          command: bundle install --path vendor/bundle

      - run:
          name: Install CocoaPods
          command: |
            curl https://cocoapods-specs.circleci.com/fetch-cocoapods-repo-from-s3.sh | bash -s cf
            pod install --verbose

      - save_cache:
          key: v3-{{ .Branch }}
          paths:
            - Pods
            - vendor/bundle

      - run:
          name: Test
          command: bundle exec fastlane test

workflows:
  version: 2
  test:
    jobs:
      - test
