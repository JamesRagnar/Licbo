machine:
  xcode:
    version: 8.3
dependencies:
  cache_directories:
      - "Carthage"
test:
  override:
    - bundle exec fastlane test
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/fastlane
    - cp fastlane/test_output/report.xml $CIRCLE_TEST_REPORTS/fastlane
